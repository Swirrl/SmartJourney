<h1>reports index.</h1>

<% @reports.each do |r| %>
  <%= r.uri.to_s %>,
  <%= r.description %>,
  <%= r.datetime.inspect %>,
  [<%= r.latitude %>, <%= r.longitude %>]
  <br/>
<% end %>

<hr/>
<%= link_to "report something!", new_report_path %>